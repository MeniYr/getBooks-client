import React from 'react'
//TODO check if there same properties of the interested user (same book and same user on same owner)


export default function useInterestedAlready() {
  return (
    <div>IsInterestedAlready</div>
  )
}
